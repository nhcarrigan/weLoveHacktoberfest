import { assert } from "chai";

import { IntentOptions } from "../../src/config/IntentOptions";

suite("IntentOptions", () => {
  test("is defined", () => {
    assert.isDefined(IntentOptions);
  });
});
